'Rudimentary Task List:
'1. Assign a back to each card
'2. On click flip card to reveal back
'3. If 2 cards flipped - check to see if its a match
'   -if match - stay flipped and continue game
'   -if no match - return cards to backward facing and continue.
'4. if all cards flipped MessageBox.Show("You Just Won!")

Public Class CardGame

    Private Sub CardGame_Load(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles MyBase.Load

    End Sub

End Class
